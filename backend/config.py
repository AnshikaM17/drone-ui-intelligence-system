# Simulated telemetry (replace later with drone data)

ALTITUDE = 10  # meters

# Camera properties (approx)
FOCAL_LENGTH = 800  # in pixels (approx)
SENSOR_WIDTH = 6.4  # mm (not critical for MVP)
IMAGE_WIDTH = 640

# Calibration factor (tune manually)
SCALE_FACTOR = 0.002